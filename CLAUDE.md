# 판례 지식관리소

이곳은 판례공보와 스터디 해설을 모아 **쟁점별 위키**를 만드는 개인 서고다.
자료가 들어오면 원문을 그대로 보존하고, 판례별 노트를 쓰고, 쟁점별 문서로 종합한다.
아래 폴더 이름과 규칙은 사용자가 직접 정했다. AI는 이를 임의로 바꾸지 않는다.

---

## 폴더 용도

| 폴더 | 용도 | 웹 발행 |
|---|---|---|
| `inbox/` | 방금 받은 자료가 잠깐 머무는 곳. **처리가 끝나면 비어 있다** | 아니오 |
| `archive/court_reports/` | 판례공보 **원본 파일**(PDF·HWP) 보관소 | 아니오 |
| `cases/` | 판례 원문을 옮긴 **마크다운 본문**. 읽고 인용하는 자리 | 아니오 |
| `case_notes/` | 판례 한 건에 대한 **내 노트**. 내 사고가 적히는 곳 | 아니오 |
| `archive/literature/` | 스터디 해설·논문의 **원본 파일** 보관소 | 아니오 |
| `literature/` | 문헌을 옮긴 **마크다운 본문**. 읽고 인용하는 자리 | 아니오 |
| `topics/` | 여러 판례·문헌을 가로지른 **쟁점별 종합 문서**. 최종 산출물 | **예** |
| `reports/` | 특정 목적으로 뽑아낸 **결과물**(검토 메모, 요약본 등) | 아니오 |

폴더 이름은 **고정**이다. 설치된 스킬들이 이 이름을 전제로 동작하므로 바꾸거나 새 폴더를 만들지 않는다.
웹 발행은 `_config.yml`의 `exclude`로 통제한다. **AI는 그 목록을 임의로 바꾸지 않는다.** (`exclude`는 사이트 렌더링만 막을 뿐, 저장소가 공개되어 있으면 파일 자체는 열람 가능하다.)

### 세 층의 구분

|  | 원본 파일 | 마크다운 본문 | 내 해석 |
|---|---|---|---|
| **판례** | `archive/court_reports/` | `cases/` | `case_notes/` |
| **문헌** | `archive/literature/` | `literature/` | (없음) |

- **`archive/`는 원본 파일 보관소다.** 인용의 근거이며 변형되지 않는다.
- **`cases/`와 `literature/`는 읽고 인용하는 마크다운 자리다.** 원본을 옮겨온 것이지 새로 쓴 것이 아니다.
- **`case_notes/`와 `topics/`는 사용자가 최종 책임지는 자리다.** AI는 초안을 쓸 수 있으나 규칙 4의 표시를 붙인다. 단 `## 내 메모` 칸은 예외로 AI가 쓰지 못한다(규칙 3).

**문헌 라인에 노트 칸이 없는 것은 의도된 설계다.** 이 서고에서 사용자의 사고는 오직 `case_notes/`의 `## 내 메모` 칸에서 시작한다. 문헌을 읽고 든 판단도 그 문헌이 걸린 판례의 노트에 적는다. 시작점이 하나이기 때문에 보호할 자리도 하나다. **이 비대칭을 결함으로 보고 "보완"하지 않는다.**

### case_notes 노트의 칸 구성

`case_notes/`의 모든 노트는 아래 여섯 칸을 이 순서로 갖는다. 칸 이름을 바꾸거나 새 칸을 만들지 않는다.

| 칸 | 쓰는 사람 | 내용 |
|---|---|---|
| `## 사실관계` | AI·사용자 | 사건의 경과 |
| `## 쟁점` | AI·사용자 | 다투어진 문제 |
| `## 판시` | AI·사용자 | 법원의 판단 |
| `## 원문 이상` | AI·사용자 | 원문에서 발견한 오탈자·판독불가 등(규칙 1) |
| `## AI 제안` | AI·사용자 | AI가 "내 메모"에 대해 하고 싶은 말(규칙 3) |
| `## 내 메모` | **사용자 전용** | 내 판단. AI 금지구역(규칙 3) |

**`## 내 메모` 칸의 경계는 `## 내 메모` 줄부터 다음 `##` 줄 직전까지, 다음 `##`가 없으면 본문 영역의 끝까지다.**

### 본문 영역의 경계

**본문 영역**은 파일의 시작(front matter가 있으면 그 다음 줄)부터 `<!-- AI-NOTICE -->` 마커 직전까지다.
이 마커는 파일당 한 번, 고지문 바로 앞에만 쓴다(규칙 9). 마커 아래는 본문이 아니다.

---

## 시스템 규칙

### 1. 판례 원문은 한 글자도 고치지 않는다

`archive/`의 모든 파일과 `cases/`·`literature/`의 **본문 영역**은 읽기 전용이다.

- 오탈자, 띄어쓰기, 옛 표기, 한자, 비문을 발견해도 고치지 않는다. 원문의 오류로 보이는 것은 **해당 판례 노트의 `## 원문 이상` 칸에 적고 원문은 그대로 둔다.**
- 옮길 때 요약·생략·현대어화·문단 재구성을 하지 않는다.
- **공백과 줄바꿈만 정리한다. 그 밖의 문자는 하나도 더하거나 빼지 않는다.**
  **확인:** 원문과 변환본에서 공백을 모두 제거하고 대조하면 완전히 일치해야 한다.
- 판독이 안 되는 부분은 추정해서 채우지 않고 `[판독불가]`로 표시하고, `## 원문 이상` 칸에 위치를 적는다.

### 2. 인용은 출처 경로를 반드시 표시한다

인용은 가능한 모든 경로로 찾는다. 서고 안의 파일, 법제처 MCP 검색, 모델 자체의 지식 모두 쓴다.

**모든 인용에는 인용 문장 바로 뒤 괄호 안에 아래 세 표시 중 정확히 하나가 붙는다.**

| 출처 | 표시 |
|---|---|
| 서고 안의 파일 | 파일 경로 — `(cases/2019다12345.md)` |
| 법제처 MCP 조회 | `(법제처 확인)` |
| 모델의 지식뿐 | `[미검증]` |

- **표시가 없는 인용은 그 자체로 위반이다.**
- **`[미검증]` 표시가 붙은 인용은 `topics/`와 `reports/`에 옮기지 않는다.**
- **`[미검증]` 표시를 지울 수 있는 것은 사용자뿐이다. AI는 어떤 경우에도 지우지 않는다.**

### 3. 노트의 "내 메모" 칸은 AI가 건드리지 않는다

`case_notes/` 각 파일의 `## 내 메모` 칸은 사용자 전용 구역이다.

- AI는 이 칸을 **읽을 수 있으나 쓰거나, 고치거나, 지우거나, 순서를 바꾸거나, 표현을 다듬지 않는다.** 덧붙이기도 금지다.
- **판정:** `git diff`에서 이 칸의 경계 안쪽에 변경이 하나라도 있으면 위반이다.
- 할 말이 있으면 **`## AI 제안` 칸에 적는다.** 옮길지는 사용자가 정한다.
- **예외:** 사용자가 이 칸을 명시적으로 지목해 수정을 요청한 경우에만 손댄다. 그때도 **원래 문장을 지우지 않고 아래에 덧붙인다.**

### 4. AI가 쓴 부분은 항상 표시한다

AI는 `## 내 메모` 칸을 제외한 모든 곳에서 초안을 먼저 쓸 수 있다. `topics/`의 결론 문장도 포함한다.

- AI가 쓴 **문단** 바로 위에 표시를 붙인다. 문서 전체가 AI 초안이면 본문 맨 위에 한 번 붙인다.

  ```
  > [AI 초안 — 미검토]
  ```

- **이 표시를 지울 수 있는 것은 사용자뿐이다. AI는 어떤 경우에도 지우지 않는다.**
  표시가 사라졌다는 것이 곧 사용자가 검토했다는 뜻이다.
- **이 표시가 붙은 문단은 인용하거나 다른 문서로 옮기지 않는다.**

### 5. 실제 소송기록으로 보이는 문서는 처리를 거부한다

이 서고는 **공개된 판례공보와 해설**을 위한 곳이다.

**징표:** 소장·답변서·준비서면·증거설명서·의견서 등의 서면 양식 / 사건번호와 당사자 실명이 함께 있는 문서 / 미공개 재판 문서 / 주민등록번호·주소·연락처 등 당사자 식별정보.

**징표가 보이면 즉시 처리를 멈춘다.**

- 그 파일의 내용을 **대화창에 옮겨 적거나, 다른 파일에 쓰거나, 요약하지 않는다.**
- **파일명과 "소송기록으로 의심된다"는 사실만 보고한다.**
- 다른 폴더로 옮기지 않고, 복사하지 않고, **삭제하지도 않는다.** `inbox/`에 그대로 둔 채 사용자의 지시를 기다린다(규칙 8의 보류 절차를 따른다).

판례공보에 실려 공개된 사건은 이 규칙에 걸리지 않는다.
**확신이 없으면 처리하지 않고 사용자에게 묻는다.**

### 6. 사용자의 신상·직무 정보는 묻지도 기록하지도 않는다

자료가 익명이어도 **자료의 선택은 익명이 아니다.**

- AI는 사용자의 **이름, 소속, 직위, 담당 사건, 근무지, 연락처를 묻지 않는다.**
- 대화 중 알게 되더라도 **어떤 파일에도 적지 않는다.**
- **예시나 서식에는 실제 기관명, 부서명, 사건번호, 인명, 날짜를 넣지 않는다.** `○○법원`, `2000가합0000` 같은 가상 표기를 쓴다.
- 문서에 필요한 것은 **자료의 출처이지 작성자의 신원이 아니다.**

### 7. 지우거나 바꾸는 일은 반드시 사전 확인을 받는다

| 행위 | 확인 |
|---|---|
| 새 파일 만들기 | **불필요** |
| 기존 파일에 내용 **덧붙이기** | **불필요** |
| 기존 내용 **지우기·바꾸기** | **필요** |
| 파일 삭제·이동·전체 교체 | **필요** |
| `## 내 메모` 칸에 손대기 | **금지**(규칙 3) |

`inbox/`를 비우는 작업도 예외가 아니다.

### 8. 처리를 마치면 inbox를 비운다

| 무엇을 | 어디로 |
|---|---|
| 판례 마크다운 | `cases/` |
| 문헌 마크다운 | `literature/` |
| 판례 원본 파일(PDF·HWP) | `archive/court_reports/` |
| 문헌 원본 파일(PDF·HWP) | `archive/literature/` |

- **규칙 7에 따라, 옮기기 전에 위 표 형식으로 이동 계획을 제시하고 승인을 받는다.**
- **복사가 아니라 이동이다.** `inbox/`에 사본을 남기지 않는다.
- **처리를 마치면 `inbox/`에는 아무것도 남지 않는다.** 남겨야 하는 파일이 있으면 **같은 이름에 `.보류.md`를 붙인 파일**을 나란히 만들고 사유를 한 줄 적는다.
  예: `2019다12345.pdf` → `2019다12345.pdf.보류.md`
- **판정:** `inbox/`의 모든 파일은 짝이 되는 `.보류.md`를 가져야 한다. **짝이 없는 파일은 미처리다.**

### 9. 모든 산출물 말미에 AI 고지문을 붙인다

`cases/`, `literature/`, `case_notes/`, `topics/`, `reports/`에서 AI가 만들거나 수정한 모든 마크다운 파일 **맨 끝**에 붙인다.

```markdown
<!-- AI-NOTICE -->

---
*이 문서는 AI(Claude)의 도움으로 작성되었습니다.*
*판례 원문과 인용은 반드시 원본으로 확인하시기 바랍니다. 법률 자문이 아닙니다.*
*최종 갱신: YYYY-MM-DD*
```

- **`archive/`에는 붙이지 않는다.** 원본 파일에 손대는 것이 되기 때문이다.
- `<!-- AI-NOTICE -->` 마커가 본문 영역의 끝을 정한다. 규칙 1의 원문 불변 의무는 마커 위쪽에만 적용되므로, 고지문을 붙이는 것은 원문을 고치는 것이 아니다.
- 고지문은 **파일 단위**, 규칙 4의 `[AI 초안 — 미검토]`는 **문단 단위**의 표시다. 층이 다르므로 둘 다 필요하다.

---

**규칙을 바꾸려면:** AI는 이 규칙을 임의로 추가·삭제·완화하지 않는다. 규칙 사이에 충돌이 보이면 혼자 해석해 처리하지 말고 **사용자에게 알리고 결정을 받는다.**
